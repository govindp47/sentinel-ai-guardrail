name: deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy placeholder
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy placeholder
        run: echo "Deploy placeholder — real steps added in Phase 7 (T-070)."
